import { NextResponse } from "next/server";

// Stub — Phase 3 will implement Classy donation webhook/redirect handling
export async function POST() {
  return NextResponse.json({ status: "stub" }, { status: 200 });
}
