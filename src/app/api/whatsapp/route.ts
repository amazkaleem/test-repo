import { NextResponse } from "next/server";

// Stub — Phase 3 will implement WhatsApp broadcast signup via WATI or Twilio
export async function POST() {
  return NextResponse.json({ status: "stub" }, { status: 200 });
}
